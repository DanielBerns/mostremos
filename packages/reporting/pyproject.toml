[project]
name = "reporting"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "aiohttp>=3.9.0",
    "structlog>=24.1.0",
]

[build-system]
requires = ["uv_build>=0.10.2,<0.11.0"]
build-backend = "uv_build"
