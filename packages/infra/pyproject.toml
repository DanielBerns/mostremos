[project]
name = "infra"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "core",
    "sqlalchemy>=2.0.0",
    "structlog>=24.1.0",
    "werkzeug"
]

[tool.uv.sources]
core = { workspace = true }

[build-system]
requires = ["uv_build>=0.10.2,<0.11.0"]
build-backend = "uv_build"
